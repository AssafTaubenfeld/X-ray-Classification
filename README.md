Stracture:

cnn_impl and vit_impl contains architectue specific code.
train, dataset, test_flow and base_config are shared for both architectures. 

main.ipynb conains all the experimintation done in this project, with the results.


To train and run the code, follow the logic in the main.ipynb file
